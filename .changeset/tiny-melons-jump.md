---
"executor": patch
---

Improve packaged release reliability with a bootstrap smoke test and lazy-load the 1Password SDK so startup does not require its wasm assets unless the feature is actually used.
